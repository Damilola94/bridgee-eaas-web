const prefix = '';

const endpoints: any = {
  auth: `${prefix}/auth`,
  user: `${prefix}/user`,
  merchant: `${prefix}/merchant`,
  transaction: `${prefix}/transaction`
};

export default endpoints;
